#!/bin/bash

if [ ! -d ~/.password-store ]; then
    echo "No pass store found" 1>&2
    echo "Run setup-secret-store.sh to setup secret management"
    echo "See docs/secrets.sh"
    exit 0
fi

#for secret in $(pass ls Dotfiles | grep └── | cut -d' ' -f2); do
#    export "$secret"="$(pass Dotfiles/$secret)"
#done
